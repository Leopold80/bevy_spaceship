[package]
name = "apollo-core"
version = "0.1.0"
edition = "2024"
description = "Backend-neutral state, action, timing, telemetry, and Apollo model definitions"

[dependencies]
glam = { version = "0.32.1", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
